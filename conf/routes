# Routes
# This file defines all application routes (Higher priority routes first)

GET      /api/v1/organizations/:orgName/contributors      controllers.ContributorController.getContributions(orgName: String)
